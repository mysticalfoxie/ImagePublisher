import {EnvironmentModel} from "../models/environment.model";

// @ts-ignore -> This file is being replaced anyways. It's just for reference within the project.
export const environment: EnvironmentModel;
